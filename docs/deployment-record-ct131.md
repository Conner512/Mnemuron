# Mnemuron Deployment Evidence

> Public specification and example configuration only. Personal deployment records and acceptance evidence are retained privately. This document is not proof of production readiness; `production_ready=false` remains the release boundary.

The historical filename is retained for link compatibility only. It does not identify a public service, host, container, or supported deployment target.

Environment-specific addresses, device and chat identities, Session/Resume/Receipt IDs, backup locations, source hashes, and deployment timelines are not published here.

Use the [deployment guide](pve-lxc-deployment-v0.1.md) for configuration and the [production acceptance matrix](production-readiness-evidence-matrix-v0.1.md) for validation. Record actual artifact versions, runtime identity, health, persistence, and the complete Preview → exact Confirm → next ordinary turn → terminal ACK cycle in a private deployment report. Installed files or a successful local test alone do not establish real-client acceptance.
